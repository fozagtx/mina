{
  "name": "memecoin-launcher-workflow",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "cre workflow build",
    "simulate": "cre workflow simulate",
    "deploy": "cre workflow deploy"
  },
  "dependencies": {
    "@chainlink/cre-sdk": "latest",
    "viem": "^2.0.0",
    "zod": "^3.22.0"
  }
}
